import { CompletionDatePipe } from './completion-date.pipe';

describe('CompletionDatePipe', () => {
  it('create an instance', () => {
    const pipe = new CompletionDatePipe();
    expect(pipe).toBeTruthy();
  });
});
