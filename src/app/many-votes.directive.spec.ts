import { ManyVotesDirective } from './many-votes.directive';

describe('ManyVotesDirective', () => {
  it('should create an instance', () => {
    const directive = new ManyVotesDirective();
    expect(directive).toBeTruthy();
  });
});
